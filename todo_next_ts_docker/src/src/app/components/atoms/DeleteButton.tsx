import { Button } from "@chakra-ui/react";
import React from "react";

const DeleteButton = () => {
  return (
    <Button ml="10px" colorScheme="red">
      削除
    </Button>
  );
};

export default DeleteButton;
